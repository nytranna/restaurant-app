import { Extension, Naja } from '../Naja';
declare module '../Naja' {
    interface Options {
        abort?: boolean;
    }
}
export declare class AbortExtension implements Extension {
    initialize(naja: Naja): void;
    private readonly abortControllers;
    private onInitialize;
    private checkAbortable;
    private saveAbortController;
    private removeAbortController;
}
