import { Naja } from '../Naja';
export declare class ScriptLoader {
    private readonly naja;
    private loadedScripts;
    private static parser;
    constructor(naja: Naja);
    private initialize;
    loadScripts(content: string): void;
    loadScripts(snippets: Record<string, string>): void;
    private loadScriptsInSnippet;
}
